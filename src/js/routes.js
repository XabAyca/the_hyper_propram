import {PageList,searchGame} from './PageList'; 
import PageDetail from './PageDetail';

const routes = {
  "": PageList,
  "pagelist": PageList,
  "pagedetail": PageDetail,
};

export default routes 